import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Toss 브랜드 색상
  static const Color primary = Color(0xFF0064FF);
  static const Color primaryLight = Color(0xFF4C8BFF);
  static const Color primaryDark = Color(0xFF0047B3);

  // 배경 색상
  static const Color background = Color(0xFFFAFAFA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF5F5F5);

  // 텍스트 색상
  static const Color textPrimary = Color(0xFF191F28);
  static const Color textSecondary = Color(0xFF8B95A1);
  static const Color textTertiary = Color(0xFFBEC3CC);
  static const Color textDisabled = Color(0xFFD1D6DE);

  // 상태 색상
  static const Color success = Color(0xFF00D4AA);
  static const Color warning = Color(0xFFFF9500);
  static const Color error = Color(0xFFFF3B30);
  static const Color info = Color(0xFF0064FF);

  // 구분선 색상
  static const Color divider = Color(0xFFF2F4F6);
  static const Color border = Color(0xFFE5E8EB);

  // 그림자 색상
  static const Color shadow = Color(0x1A000000);
  static const Color shadowLight = Color(0x0A000000);

  // 하단 네비게이션 색상
  static const Color bottomNavBackground = Color(0xFFFFFFFF);
  static const Color bottomNavSelected = Color(0xFF0064FF);
  static const Color bottomNavUnselected = Color(0xFF8B95A1);
}
